<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

<p id="notice"><%= notice %></p>

<h1>Products</h1>

<%= form_tag products_path, :method => 'get' do %>
  <p>
    <%= text_field_tag :search, params[:search], id: 'search' %>
    <%= submit_tag "Search", :name => nil %>
    <%= button_tag 'CLEAR', id: 'clear_form_button' %>
  </p>
<% end %>
  <%#= form_with(model: @product) do |form| %>
    <!-- <div class="field"> -->
      <%#= form.label :name %>
      <%#= content_tag :search_1, params[:search_1], id: 'search_1', 'data-controller': 'scanner' do %>
        <%#= form.text_field :name, class: 'form-control', 'data-scanner-target': 'output' %>
      <%# end %>
      <%#= submit_tag "Search", :name => nil %>

      <%#= link_to 'Show', product_path(@product) %>
    <!-- </div> -->
  <%# end %>

  <%#= button_to "Show", product_url(@product), :id => "open-contacts-dialog-btn",
      :class => "inbox-sf-add-btn tip", :method => :get %>
    <%#= link_to "Show",  product_url('data-scanner-target': 'output') %>




<%= javascript_pack_tag 'clear' %>



<table class='table'>
  <thead>
    <tr>
      <th>Name</th>
      <th>ID</th>
      <th>Qty</th>
      <th>QR-Code</th>
      <th colspan="3"></th>

    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.id %></td>
        <td><%= product.qty %></td>
        <td><%= QrCode.call(product_url(product))%></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path %>



